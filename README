# AIbot

AIbot is a Python-based project designed to process and embed documents using OpenAI's embedding models and store them in a FAISS vector database for efficient retrieval.

## Features

- Processes documents in PDF format.
- Splits documents into text chunks.
- Generates embeddings using OpenAI's API.
- Stores embeddings in a FAISS vector store for semantic search.

## Prerequisites

- Python 3.7+
- OpenAI API key with sufficient quota

## Installation

1. Clone the repository:

   ```bash
   git clone https://github.com/tomerdolev/AIbot.git
   cd AIbot
